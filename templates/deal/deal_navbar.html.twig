{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light deal-navbar">
        <div class="container">
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("deal_index") }}">A la une</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("deal_hot") }}">Hot</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}