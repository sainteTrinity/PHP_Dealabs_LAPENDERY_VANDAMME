{% extends 'base.html.twig' %}

{% block title %}PromoCode{% endblock %}

{% block body %}
    <h1>PromoCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo_code.id }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ promo_code.link }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ promo_code.code }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ promo_code.Title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ promo_code.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('promo_code_index') }}">back to list</a>

    <a href="{{ path('promo_code_edit', {'id': promo_code.id}) }}">edit</a>

    {{ include('promo_code/_delete_form.html.twig') }}
{% endblock %}
